<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/>
    </parent>

    <groupId>pro.abgrid.examples</groupId>
    <artifactId>alfresco-community-spring-services-quickstart</artifactId>
    <version>1.0.0</version>
    <name>alfresco-community-spring-services-quickstart</name>
    <description>Runnable Spring Boot example for alfresco-community-spring-services</description>

    <properties>
        <java.version>21</java.version>
        <alfresco-community-spring-services.version>1.0.0</alfresco-community-spring-services.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>pro.abgrid</groupId>
            <artifactId>alfresco-community-spring-services</artifactId>
            <version>${alfresco-community-spring-services.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
